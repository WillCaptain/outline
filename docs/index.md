# Outline 文档索引（用户 / SDK / 二次开发）

本目录聚焦如何使用 `outline` 模块，不覆盖底层实现细节。

## 快速开始

- `quickstart.md`：从依赖引入到解析一段 Outline 代码的最短路径。

## 语言文档

- `cheatsheet.md`：一页速查表，涵盖所有语法形式与特色能力。
- `outline-language.md`：Outline 语法与语言特性手册。
- `type-entity-extension.md`：type/entity 扩展时同名成员的默认、`override`、`overload` 规则。

## SDK 与集成

- `sdk-integration.md`：`OutlineParser` API、单模块与多模块解析模式、与 GCP 的集成方式。
